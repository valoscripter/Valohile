local player = game.Players.LocalPlayer
local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/KaanFriztche/SydearrHubLibrary/refs/heads/main/SydearrHubLibraryMobile')))()
local Window = OrionLib:MakeWindow({
    Name = "Sydearr Hub - TA  V1.6.4",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "OrionTest"
})

-- Create a tab
local Tab1 = Window:MakeTab({
    Name = "▶┆  Anasayfa",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Add a section to the tab
local Section1 = Tab1:AddSection({
    Name = "- UI Anasayfası -"
})

Tab1:AddParagraph("Sydearr Hub – TA V1.6.4 Büyük Güncelleme! ", [[
 
▶┆ Oto Parkur Yapma Eklendi.
 
▶┆ Araba Sürme Eklendi.
 
▶┆ Lag Server Eklendi.
 
▶┆ Modlu Glock-17 Eklendi.

▶┆ Anti-Track Geliştirildi.

▶┆ Kill All İçin Menü Eklendi.

▶┆ Bring Players Eklendi.

▶┆ Yüz Değiştirme Eklendi.

▶┆ NPC Kontrol Eklendi.

▶┆ Animasyon Toolları Eklendi.

▶┆ THO Para Hilesi Eklendi.

▶┆ TPT Eğitim Açma Eklendi.

▶┆ Basit Kamplar İçin Team Changer Eklendi.

▶┆ TA Sahte Çark Çevirme Eklendi.

▶┆ TA Sahte Transfer Eklendi.

▶┆ Sahte Sohbet Mesajı Eklendi.

▶┆ Hitbox Expander Eklendi.

▶┆ Aimbot Eklendi.

▶┆ Silent Aim Eklendi.

•┆ Not: Bu hile tamamen ücretsizdir, para ile satan olur ise bizimle iletişime geçin.



]])





Tab1:AddParagraph("UI Developers:", [[
 
•┆ @kaansfriztche

•┆ @ross_x1

•┆ @chesterfield09

•┆ @sametaslan37373

•┆ @krafi_q
]])

Tab1:AddParagraph("Content Creators:", [[

•┆ @xenty1111 (Saygılarımızla.)
]])












-- Create a tab
local Tab2 = Window:MakeTab({
    Name = "▶┆ OP & Rütbe",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Add a section to the tab
local Section2 = Tab2:AddSection({
    Name = "- Jackler -"
})

-- Create a notification
OrionLib:MakeNotification({
    Name = "Hoşgeldin!",
    Content = "Sydearr Hub'a Hoşgeldin!",
    Image = "rbxassetid://4483345998",
    Time = 8
})


Tab2:AddButton({
    Name = "• Oto JJ Çek! Tool     (1 - 10.000)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/JJobf.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab2:AddButton({
    Name = "• Oto GJ Çek! Tool     (1 - 10.000)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/GJobf.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab2:AddButton({
    Name = "• Oto HJ Çek! Tool     (1 - 10)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/HJ%C3%87ek"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




-- Tab 7'e bölüm ekleniyor
local Section2 = Tab2:AddSection({
    Name = "- İşinize Yarar -"
})

Tab2:AddButton({
    Name = "• Oto Alım Yap",
    Callback = function()
        





Tab2:AddButton({
    Name = "• Doğru Yanlış Takım Değiştirme",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/dygecme"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya u
-- 📌 Oyunun tamamen yüklenmesini bekle
if not game:IsLoaded() then
    game.Loaded:Wait()
end

-- 📌 LocalPlayer hazır olana kadar bekle
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

while not LocalPlayer or not LocalPlayer:FindFirstChild("PlayerGui") do
    task.wait()
    LocalPlayer = Players.LocalPlayer
end

-- 📌 GUI Elemanlarını Oluşturma
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KomutSistemiGUI"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Frame.Position = UDim2.new(1, -260, 0.3, 0)
Frame.Size = UDim2.new(0, 250, 0, 240)
Frame.BorderSizePixel = 0
Frame.BackgroundTransparency = 0.1
Frame.Active = true
Frame.Draggable = true

local UICorner1 = Instance.new("UICorner", Frame)
UICorner1.CornerRadius = UDim.new(0, 10)

local SearchBox = Instance.new("TextBox", Frame)
SearchBox.PlaceholderText = "İsim ara..."
SearchBox.Text = ""
SearchBox.Position = UDim2.new(0.05, 0, 0.05, 0)
SearchBox.Size = UDim2.new(0.6, 0, 0, 30)
SearchBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
SearchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SearchBox.Font = Enum.Font.GothamBold
SearchBox.TextSize = 12

local UICorner2 = Instance.new("UICorner", SearchBox)
UICorner2.CornerRadius = UDim.new(0, 8)

local SearchButton = Instance.new("TextButton", Frame)
SearchButton.Text = "Takip Et"
SearchButton.Position = UDim2.new(0.7, 0, 0.05, 0)
SearchButton.Size = UDim2.new(0.25, 0, 0, 30)
SearchButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
SearchButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SearchButton.Font = Enum.Font.GothamBold
SearchButton.TextSize = 12

local FollowedPlayerLabel = Instance.new("TextLabel", Frame)
FollowedPlayerLabel.Text = "Takip edilen kişi: -"
FollowedPlayerLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
FollowedPlayerLabel.BackgroundTransparency = 1
FollowedPlayerLabel.Position = UDim2.new(0.05, 0, 0.25, 0)
FollowedPlayerLabel.Size = UDim2.new(0.9, 0, 0, 30)
FollowedPlayerLabel.Font = Enum.Font.GothamBold
FollowedPlayerLabel.TextSize = 12
FollowedPlayerLabel.TextXAlignment = Enum.TextXAlignment.Left

local GivenCommand = Instance.new("TextLabel", Frame)
GivenCommand.Text = "Verilen komut: -"
GivenCommand.TextColor3 = Color3.fromRGB(255, 255, 255)
GivenCommand.BackgroundTransparency = 1
GivenCommand.Position = UDim2.new(0.05, 0, 0.45, 0)
GivenCommand.Size = UDim2.new(0.9, 0, 0, 30)
GivenCommand.Font = Enum.Font.GothamBold
GivenCommand.TextSize = 12
GivenCommand.TextXAlignment = Enum.TextXAlignment.Left

local ApplyButton = Instance.new("TextButton", Frame)
ApplyButton.Text = "Komutu Uygula"
ApplyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ApplyButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ApplyButton.Position = UDim2.new(0.5, 0, 0.75, 0)
ApplyButton.Size = UDim2.new(0, 150, 0, 30)
ApplyButton.AnchorPoint = Vector2.new(0.5, 0)
ApplyButton.Font = Enum.Font.GothamBold
ApplyButton.TextSize = 12

local UICorner3 = Instance.new("UICorner", ApplyButton)
UICorner3.CornerRadius = UDim.new(0, 8)

-- 📌 Cache
local followedPlayer = nil
local lastCommand = nil
local commandLocked = false
local controlling = false

-- 📌 Komutlar
local commandActions = {
    ["Sağ yüz."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-90), 0) end end,
    ["Sağ yüz!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-90), 0) end end,
    ["Sol yüz."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(90), 0) end end,
    ["Sol yüz!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(90), 0) end end,
    ["Arka yüz."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(180), 0) end end,
    ["Arka yüz!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(180), 0) end end,
    ["Sağa dön."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-90), 0) end end,
    ["Sağa dön!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-90), 0) end end,
    ["Sola dön."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(90), 0) end end,
    ["Sola dön!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(90), 0) end end,
    ["Geriye dön."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(180), 0) end end,
    ["Geriye dön!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(180), 0) end end,
    ["Sağ çapraz."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0) end end,
    ["Sağ çapraz!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0) end end,
    ["Sol çapraz."] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(45), 0) end end,
    ["Sol çapraz!"] = function(char) if not commandLocked then char.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(45), 0) end end,
    ["Merkez yüz."] = function(char, source)
        commandLocked = false
        if source and source.Character and source.Character:FindFirstChild("HumanoidRootPart") then
            local dir = source.Character.HumanoidRootPart.Position - char.HumanoidRootPart.Position
            char.HumanoidRootPart.CFrame = CFrame.new(char.HumanoidRootPart.Position, char.HumanoidRootPart.Position + Vector3.new(dir.X, 0, dir.Z))
        end
    end,
    ["Merkez yüz!"] = function(char, source) commandActions["Merkez yüz."](char, source) end,
    ["Kilit yüz."] = function(char) commandLocked = true controlling = false end,
    ["Kilit yüz!"] = function(char) commandLocked = true controlling = false end,
    ["Kontrol yüz."] = function(char, source)
        commandLocked = false
        controlling = true
        coroutine.wrap(function()
            while controlling and followedPlayer and followedPlayer.Character and followedPlayer.Character:FindFirstChild("HumanoidRootPart") do
                local root = char:FindFirstChild("HumanoidRootPart")
                local targetRoot = followedPlayer.Character.HumanoidRootPart
                local distance = (root.Position - targetRoot.Position).Magnitude
                if distance < 7 then -- Çok yakınsa anında hızlı dön
                    local dir = targetRoot.Position - root.Position
                    root.CFrame = CFrame.new(root.Position, root.Position + Vector3.new(dir.X, 0, dir.Z))
                elseif distance < 70 then -- Normal mesafe
                    local dir = targetRoot.Position - root.Position
                    root.CFrame = CFrame.new(root.Position, root.Position + Vector3.new(dir.X, 0, dir.Z))
                else -- Uzaksa hızlı hızlı etrafında dön
                    root.CFrame *= CFrame.Angles(0, math.rad(60), 0)
                end
                task.wait(0.05)
            end
        end)()
    end,
    ["Kontrol yüz!"] = function(char, source) commandActions["Kontrol yüz."](char, source) end,
}

-- 📌 Komut kontrol
local function checkMessage(message)
    if not message then return end
    message = message:match("^%s*(.-)%s*$")
    if commandActions[message] then
        lastCommand = message
        GivenCommand.Text = "Verilen komut: " .. message
    end
end

-- 📌 Takip edilen oyuncuya bağlan
local function followPlayer(player)
    if player then
        player.Chatted:Connect(function(msg)
            checkMessage(msg)
        end)
    end
end

-- 📌 Butonlar
SearchButton.MouseButton1Click:Connect(function()
    local text = SearchBox.Text:lower()
    for _, player in ipairs(Players:GetPlayers()) do
        if player.Name:lower():find(text) then
            followedPlayer = player
            FollowedPlayerLabel.Text = "Takip edilen kişi: " .. player.Name
            followPlayer(player)
            break
        end
    end
end)

ApplyButton.MouseButton1Click:Connect(function()
    if lastCommand and followedPlayer then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local action = commandActions[lastCommand]
            if action then
                action(char, followedPlayer)
                print("Komut uygulandı:", lastCommand)
            end
        end
        lastCommand = nil
        GivenCommand.Text = "Verilen komut: -"
    end
end)
})


Tab2:AddButton({
    Name = "• Sahte OP",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/FakeOp.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab2:AddButton({
    Name = "• Sydearr Sohbet Alanı",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/valoscripter/Salamsucuksosis/refs/heads/main/SydearrChat.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab2:AddButton({
    Name = "• Araba Sürme",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Araba-S%C3%BCrme"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab2:AddButton({
    Name = "• Oto Parkur Yapma",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/ParkurYapma.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab2:AddButton({
    Name = "• General Listesi",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/GeneralListe.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab2:AddButton({
    Name = "• Herkesi AS.İZ Yapma (Asteğmen +)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Herkesi-AS.IZ-YAPMA"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





-- Tab 7'e bölüm ekleniyor
local Section2 = Tab2:AddSection({
    Name = "- OP Kasma -"
})


Tab2:AddButton({
    Name = "• Tepeköy Farm   (24 Saatte 1M OP)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Tepek%C3%B6y.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})


Tab2:AddButton({
    Name = "• Çöl Farm   (6 Saatte 1M OP)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/C%C3%B6lFarm.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



-- Tab 7 oluşturuluyor
local Tab7 = Window:MakeTab({
    Name = "▶┆ Troller",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Tab 7'e bölüm ekleniyor
local Section7 = Tab7:AddSection({
    Name = "- Animasyonlu -"
})


-- Add TSK BASE button to Tab 3





Tab7:AddButton({
    Name = "• R6 Bang FE",
    Callback = function()
        local scriptUrl = "https://pastebin.com/raw/aPSHMV6K"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab7:AddButton({
    Name = "• R6 Suck Dick FE",
    Callback = function()
        local scriptUrl = "https://pastebin.com/raw/FPu4e2Qh"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})

Tab7:AddButton({
    Name = "• R6 Get Fucked FE",
    Callback = function()
        local scriptUrl = "https://pastebin.com/raw/zHbw7ND1"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})













Tab7:AddButton({
    Name = "• R6 & R15 Hug FE",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/JSFKGBASDJKHIOAFHDGHIUODSGBJKLFGDKSB/fe/refs/heads/main/FEHUGG"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})


Tab7:AddButton({
    Name = "• R6 Animations FE",
    Callback = function()
        local scriptUrl = "https://pastebin.com/raw/1p6xnBNf"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})

Tab7:AddButton({
    Name = "• R6 SCP-096 FE",
    Callback = function()
        local scriptUrl = "https://pastefy.app/YsJgITXR/raw"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})


Tab7:AddButton({
    Name = "• R6 Fighter FE ( Punch & Defense)",
    Callback = function()
        local scriptUrl = "https://pastefy.app/wxVAgZpT/raw"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})


-- Tab 7'e bölüm ekleniyor
local Section7 = Tab7:AddSection({
    Name = "- Troller -"
})







Tab7:AddButton({
    Name = "• R6 Touch Fling FE",
    Callback = function()
        local scriptUrl = "https://pastefy.app/59mJGQGe/raw"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})


Tab7:AddButton({
    Name = "• R6 Invisible FE",
    Callback = function()
        local scriptUrl = "https://pastebin.com/raw/3Rnd9rHf"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})









Tab7:AddButton({
    Name = "• Anti - Track",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Anti-Track.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab7:AddButton({
    Name = "• Görünmez Ol (Sydearr Made)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/G%C3%B6r%C3%BCnmezOl.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})

Tab7:AddButton({
    Name = "• Kill All Menü (Öldürme Yetkisi)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/KillAllGui.txt"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab7:AddButton({
    Name = "• Bring Players",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Rejoin"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab7:AddButton({
    Name = "• Fling All",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/flingall.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab7:AddButton({
    Name = "• Detaine Yakalanmama",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/DetaineYakalanmama.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})


Tab7:AddButton({
    Name = "• Lag Server",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/LagServer"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab7:AddButton({
    Name = "• Modlu Glock-17",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/ModluGlock.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})


Tab7:AddButton({
    Name = "• Yüz Değiştirme FE",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Y%C3%BCz-De%C4%9Fi%C5%9Ftirme.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab7:AddButton({
    Name = "• Silah Modifikasyonları",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/gunmods.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab7:AddButton({
    Name = "• Hasar Almama",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/HasarAlmama.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab7:AddButton({
    Name = "• NPC Kontrol",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/randomstring0/Qwerty/refs/heads/main/qwerty38.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})














-- Tab 9 oluşturuluyor
local Tab9 = Window:MakeTab({
    Name = "▶┆ Toollar",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Tab 7'e bölüm ekleniyor
local Section9 = Tab9:AddSection({
    Name = "- Toollar Scriptleri -"
})


-- Add TSK BASE button to Tab 3




Tab9:AddButton({
    Name = "• R6 Drink Cola Tool FE",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/ShutUpJamesTheLoser/ferobloxscriptdrink/refs/heads/main/fedrinkscriptroblox2025"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
       warn("Script yüklenemedi!")
        end
    end
})


Tab9:AddButton({
    Name = "• R6 31 Çek Tool FE",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://pastefy.app/wa3v2vgm/raw"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab9:AddButton({
    Name = "• R6 Animasyon Toolları",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://rawscripts.net/raw/Universal-Script-Epik-R6-Dancezz-42246"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab9:AddButton({
    Name = "• Telekinesis Tool FE",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://raw.githubusercontent.com/randomstring0/Qwerty/refs/heads/main/qwerty1.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab9:AddButton({
    Name = "• R6 Mevlana Tool FE",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/mevlana"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab9:AddButton({
    Name = "• R6 Teleport Tool FE",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/ToTool.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})


Tab9:AddButton({
    Name = "• R6 To Tool FE",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/TeleportTool.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



local Tab4 = Window:MakeTab({
    Name = "▶┆ Teleportlar",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Add a section to the tab
local Section4 = Tab4:AddSection({
    Name = "- Metro İstasyonları -"
})

-- Create a notification
OrionLib:MakeNotification({
    Name = "Hoşgeldin!",
    Content = "Sydearr Hub'a Hoşgeldin!",
    Image = "rbxassetid://4483345998",
    Time = 8
})


           
Tab4:AddButton({
    Name = "• Denetim Alanı",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1145.24, 13.76, -255.10)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})



-- Add TSK BASE button to Tab 3
Tab4:AddButton({
    Name = "• Düşman Adası",
    Callback = function()
        local teleportLocations = {
            MetroDoor4 = Vector3.new(-2037.29, 17.98, -364.72)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("MetroDoor4")
    end
})



-- Add TSK BASE button to Tab 3
Tab4:AddButton({
    Name = "• Kafeterya",
    Callback = function()
        local teleportLocations = {
            MetroDoor3 = Vector3.new(2927.18, 105.48, 536.67)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("MetroDoor3")
    end
})





Tab4:AddButton({
    Name = "• Safe Zone",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(2641.71, 31.82, 198.66)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})



Tab4:AddButton({
    Name = "• Sivil Base",
    Callback = function()
        local teleportLocations = {
            MetroDoor3 = Vector3.new(985.43, 13.74, -1277.78)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("MetroDoor3")
    end
})





Tab4:AddButton({
    Name = "• TSK Base",
    Callback = function()
        local teleportLocations = {
            MetroDoor = Vector3.new(3077.62, 34.01, -252.62)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("MetroDoor")
    end
})




-- Add TSK BASE button to Tab 3
Tab4:AddButton({
    Name = "• HKK Base",
    Callback = function()
        local teleportLocations = {
            MetroDoor2 = Vector3.new(1278.45, 33.45, 1510.48)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
   end
        end

        teleportTo("MetroDoor2") 
    end
})



local Section4 = Tab4:AddSection({
    Name = "- Base İçi Teleport -"
})





Tab4:AddButton({
    Name = "• Hava Kuvvetleri Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(867.19, 34.05, 1641.80)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})




Tab4:AddButton({
    Name = "• Kara Kuvvetleri Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1481.57, 14.01, 778.00)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})




Tab4:AddButton({
    Name = "• Ordu Subayları Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(3136.84, 35.33, -218.44)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})




Tab4:AddButton({
    Name = "• Sınır Müfettişleri Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(858.55, 14.01, -953.55)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})





Tab4:AddButton({
    Name = "• ÖKB Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1389.01, 14.30, -559.90)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})






Tab4:AddButton({
    Name = "• ÖKK Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1459.73, 14.30, -559.84)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})





Tab4:AddButton({
    Name = "• Jandarma Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(884.84, 13.88, 212.98)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})










Tab4:AddButton({
    Name = "• Askeri İnzibat Base İçi",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1490.47, 13.95, -1126.18)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})




local Section4 = Tab4:AddSection({
    Name = "- Base Önü Teleport -"
})



Tab4:AddButton({
    Name = "• Hava Kuvvetleri Base Önü",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1206.28, 33.45, 1586.71)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})



Tab4:AddButton({
    Name = "• Kara Kuvvetleri Base Önü",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1265.66, 13.76, 619.76)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})


Tab4:AddButton({
    Name = "• Sınır Müfettişleri Base Önü",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(664.41, 13.76, -975.37)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})




Tab4:AddButton({
    Name = "• Özel Kuvvetler Base Önü",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1265.38, 13.76, -490.94)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})



Tab4:AddButton({
    Name = "• Jandarma Base Önü",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(663.62, 13.76, 256.96)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})



Tab4:AddButton({
    Name = "• Askeri İnzibat Base Önü",
    Callback = function()
        local teleportLocations = {
            TPart = Vector3.new(1265.04, 13.76, -947.38)
        }

        -- Karakter ve HumanoidRootPart'ı kontrol et
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

        local function teleportTo(locationName)
            local destination = teleportLocations[locationName]
            if destination then
                humanoidRootPart.CFrame = CFrame.new(destination)
            else
                warn("Geçersiz konum: " .. tostring(locationName))
            end
        end

        teleportTo("TPart")
    end
})



local Tab6 = Window:MakeTab({
    Name = "▶┆ Tüm Kamplar",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})


local Section6 = Tab6:AddSection({
    Name = "- Tüm Kamp Scriptleri -"
})





Tab6:AddButton({
    Name = "• THO Para Hilesi",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/THOParaHilesi.txt"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab6:AddButton({
    Name = "• TPT Eğitim Açma",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/TPTegitim.txt"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab6:AddButton({
    Name = "• Team Changer",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Free-Kamp-Team-Changer"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab6:AddButton({
    Name = "• TA Sahte Çark Çevirme",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/SahteCark.txt"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab6:AddButton({
    Name = "• Sahte Sohbet Mesajı",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/SahteMesaj.txt"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})





Tab6:AddButton({
    Name = "• TA Sahte Transfer",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/SahtegTransfer.txt"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})






local Tab8 = Window:MakeTab({
    Name = "▶┆ Discord",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})


local Section8 = Tab8:AddSection({
    Name = "- Bize Buralardan Ulaşabilirsiniz -"
})





Tab8:AddButton({
    Name = "• Sydearr Team Linkini Kopyala.",
    Callback = function()
        setclipboard("https://discord.gg/MNpCQj6tXE")
        toclipboard("https://discord.gg/MNpCQj6tXE")
  end    
})






Tab8:AddParagraph("UI Developers:", [[
 
•┆ @kaansfriztche

•┆ @ross_x1

•┆ @chesterfield09

•┆ @sametaslan37373

•┆ @krafi_q
]])

Tab8:AddParagraph("Content Creators:", [[

•┆ @xenty1111 (Saygılarımızla.)
]])







-- Tab 4 oluşturuluyor
local Tab5 = Window:MakeTab({
    Name = "▶┆ Diğer",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Tab 4'e bölüm ekleniyor
local Section5 = Tab5:AddSection({
    Name = "- Diğer Scriptler -"
})

-- Infinite Yield admin butonunu ekliyoruz
Tab5:AddButton({
    Name = "• İnfinite Yield Admin",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})








Tab5:AddButton({
    Name = "• Hitbox Expander",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/HitboxExpander"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})











Tab5:AddButton({
    Name = "• Anti-AFK",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/Genesisuii/Genesisuii/refs/heads/main/Anti%20AFK%20script"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})











Tab5:AddButton({
    Name = "• Aimbot",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Aimbot"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab5:AddButton({
    Name = "• Silent Aim Menü (PC)",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/godwarekrafiq/universalscript/refs/heads/main/universalscript"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})




Tab5:AddButton({
    Name = "• Chat Draw",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://raw.githubusercontent.com/ocfi/_/refs/heads/main/a"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})



Tab5:AddButton({
    Name = "• Her Şey Sydearr",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/Her%C5%9EeySydearr.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})

Tab5:AddButton({
    Name = "• Fake Lag FE",
    Callback = function()
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/FakeLag.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})

-- Infinite Yield admin butonunu ekliyoruz
Tab5:AddButton({
    Name = "• Walkspeed Changer",
    Callback = function()
        -- İnfinite Yield scriptini CodeX ile çalıştırmak için
        local scriptUrl = "https://raw.githubusercontent.com/KaanFriztche/Open-source-/refs/heads/main/SpeedMen%C3%BC.lua"
        
        -- HttpGet komutu ile scripti almak
        local success, response = pcall(function()
            return game:HttpGet(scriptUrl)
        end)
        
        if success and response then
            -- loadstring ile scripti çalıştırıyoruz
            loadstring(response)()
        else
            -- Hata durumunda kullanıcıya uyarı veriyoruz
            warn("Script yüklenemedi!")
        end
    end
})


Tab5:AddButton({
    Name = "• Infinite Jump",
    Callback = function()
        local InfiniteJumpEnabled = true
        game:GetService("UserInputService").JumpRequest:Connect(function()
            if InfiniteJumpEnabled then
                local humanoid = game:GetService("Players").LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    humanoid:ChangeState("Jumping")
                end
            end
        end)
    end
})




Tab5:AddButton({
    Name = "• Rejoin Server",
    Callback = function()
local TeleportService = game:GetService("TeleportService")
local Player = game.Players.LocalPlayer

TeleportService:Teleport(game.PlaceId, Player)
end

})


Tab5:AddButton({
    Name = "• FPS Booster (Potato Graphics)",
    Callback = function()
        -- Grafik düşürme fonksiyonu
        local function optimizeGraphics()
            -- Gereksiz parçacık efektlerini kaldır
            for _, obj in pairs(game:GetDescendants()) do
                if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then
                    obj:Destroy()
                elseif obj:IsA("BasePart") then
                    obj.Material = Enum.Material.SmoothPlastic
                    obj.CastShadow = false
                    obj.Reflectance = 0 -- Yansımaları kapat
                elseif obj:IsA("Decal") then
                    obj:Destroy()
                end
            end

            -- Aydınlatma ayarlarını optimize et
            local lighting = game:GetService("Lighting")
            lighting.GlobalShadows = false
            lighting.FogEnd = 1e9 -- Sonsuz görüş alanı
            lighting.Brightness = 1 -- Minimum parlaklık
            lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128) -- Nötr ortam ışığı
        end

        -- Optimizasyonu başlat
        optimizeGraphics()

        -- Yeni eklenen nesneleri optimize et
        game.DescendantAdded:Connect(function(obj)
            if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then
                obj:Destroy()
            elseif obj:IsA("BasePart") then
                obj.Material = Enum.Material.SmoothPlastic
                obj.CastShadow = false
                obj.Reflectance = 0
            elseif obj:IsA("Decal") then
                obj:Destroy()
            end
        end)

        -- Bildirim gönder
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "FPS Booster",
            Text = "Grafikler başarıyla düşürüldü.",
            Duration = 5
        })
    end
})
